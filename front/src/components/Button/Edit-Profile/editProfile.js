import './editProfile.css';
import createNewButton from '../button';

const createEditProfileFormImageBtn = (className, text, id) => createNewButton(className, text, id);

export default createEditProfileFormImageBtn;