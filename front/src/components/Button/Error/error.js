import './error.css';
import createNewButton from '../button';

const createErrorNotificationCloseBtn = (className, text) => createNewButton(className, text);

export default createErrorNotificationCloseBtn;