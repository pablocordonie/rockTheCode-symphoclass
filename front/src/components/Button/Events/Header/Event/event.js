import './event.css';
import createNewButton from '../../../button';

const createEventsNavCreateEventBtn = (className, text, id) => createNewButton(className, text, id);

export default createEventsNavCreateEventBtn;