import './close.css';
import createNewContainer from '../../div';

const createEventCreatorCloseIconContent = (className) => createNewContainer(className);

export default createEventCreatorCloseIconContent;