import './team.css';
import createNewImage from '../image';

const createAboutTeamMemberImage = (className, srcText, altText) => createNewImage(className, srcText, altText);

export default createAboutTeamMemberImage;