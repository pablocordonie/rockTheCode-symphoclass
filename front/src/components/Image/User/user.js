import './user.css';
import createNewImage from '../image';

const createEventsNavUserAvatar = (className, srcText, altText) => createNewImage(className, srcText, altText);

export default createEventsNavUserAvatar;