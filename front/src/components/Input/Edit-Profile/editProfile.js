import './editProfile.css';
import createNewInput from '../input';

const createEditProfileFormInput = (className, id, name, placeholderText, type) => createNewInput(className, id, name, placeholderText, type);

export default createEditProfileFormInput;