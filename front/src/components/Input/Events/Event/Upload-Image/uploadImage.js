import './uploadImage.css';
import createNewInput from '../../../input';

const createUploadImageFieldInput = (className, id, name, placeholderText, type) => createNewInput(className, id, name, placeholderText, type);

export default createUploadImageFieldInput;