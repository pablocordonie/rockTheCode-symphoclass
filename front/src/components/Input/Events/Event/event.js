import createNewInput from '../../input';

const createEventCreatorFieldInput = (className, id, name, placeholderText) => createNewInput(className, id, name, placeholderText);

export default createEventCreatorFieldInput;