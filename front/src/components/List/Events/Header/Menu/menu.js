import './menu.css';
import createNewUnorderedList from '../../../list';

const createEventsNavDropdownMenu = (className) => createNewUnorderedList(className);

export default createEventsNavDropdownMenu;