import './testimonials.css';
import createNewUnorderedList from '../list';

const createTestimonialsContent = (className) => createNewUnorderedList(className);

export default createTestimonialsContent;