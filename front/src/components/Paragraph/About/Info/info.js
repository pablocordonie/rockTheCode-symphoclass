import './info.css';
import createNewParagraph from '../../paragraph';

const createAboutInfoParagraph = (className, text) => createNewParagraph(className, text);

export default createAboutInfoParagraph;

