import './bio.css';
import createNewParagraph from '../../../paragraph';

const createAboutTeamMemberBio = (className, text) => createNewParagraph(className, text);

export default createAboutTeamMemberBio;