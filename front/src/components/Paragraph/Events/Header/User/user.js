import './user.css';
import createNewParagraph from '../../../paragraph';

const createEventsNavUsername = (className, text) => createNewParagraph(className, text);

export default createEventsNavUsername;