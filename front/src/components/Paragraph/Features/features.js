import './features.css';
import createNewParagraph from '../paragraph';

const createFeatureParagraph = (className, text) => createNewParagraph(className, text);

export default createFeatureParagraph;