import './role.css';
import createNewParagraph from '../../../../paragraph';

const createTestimonialRole = (className, text) => createNewParagraph(className, text);

export default createTestimonialRole;