import './testimonials.css';
import createNewSection from '../section';

const createTestimonialsSection = (className, id) => createNewSection(className, id);

export default createTestimonialsSection;